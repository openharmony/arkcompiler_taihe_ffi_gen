#!/usr/bin/bash

# Test and report code coverage.

source "$(dirname "$0")/common_lib.sh"
init_py_env

coverage run -m -p pytest
coverage run -p ./run-test ../test/integer -c
coverage run -p ./run-test ../test/string -c
coverage run -p ./run-test ../test/rgb -c
coverage combine
coverage report
