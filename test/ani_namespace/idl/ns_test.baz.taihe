enum Color {
    red = 1;
    yellow = 2;
    blue = 3;
}

function addEnum(color: Color, n: i32): i32;
function getEnum(n: i32): Color;

struct Data {
  a: String;
  b: String;
  c: i32;
}

function concatStruct(data: Data): String;
function makeStruct(a: String, b: String, c: i32): Data;

[namespace("ns_test", "baz")]