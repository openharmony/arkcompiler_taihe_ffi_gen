interface ICalculator {
    add(a: i32, b: i32): i32;
    sub(a: i32, b: i32): i32;
    getLastResult(): i32;
    reset(): void;
}

function makeCalculator(init: i32): ICalculator;
function restartCalculator(a: ICalculator): void;
