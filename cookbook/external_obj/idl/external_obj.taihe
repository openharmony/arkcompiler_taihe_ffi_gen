@!sts_inject("import {Person} from '@system/other.subsystem';")

function is_string(s: Opaque): bool;
function get_objects(): Array<Opaque>;

function processPerson(person: @sts_type("Person", "Lother/subsystem/Person;") Opaque): void;
